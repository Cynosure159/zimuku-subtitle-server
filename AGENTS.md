# AGENTS.md

本文件包含了专为 AI Agent (Gemini CLI) 设计的特定工作流和开发准则，旨在确保开发的一致性和代码质量。

## 核心开发准则

1.  **语言偏好**: 所有思考过程、计划过程、以及与用户的对话均使用**简体中文**。
2.  **环境隔离**: Python 开发**必须**在 `.venv` 虚拟环境中进行。在执行任何脚本、安装依赖或运行测试前，必须确保虚拟环境已激活。
3.  **代码风格与质量**: 
    - 保持代码简洁、高效且符合行业标准。
    - 所有核心逻辑、公共方法和重要变更必须附带恰当的中文注释。
    - **强制要求**: 每次执行单元测试前，**必须**同步执行代码规范检查 (`ruff check`) 与格式化检查 (`ruff format`)。
4.  **安全性与完整性**:
    - 严禁泄露、打印或提交任何敏感信息（如 API 密钥、私钥、凭据等）。
    - 始终遵循安全编码最佳实践。
    - 遵循 RESTful API 设计规范。

## 开发工作流

在进行任何代码修改前，请务必执行以下步骤：

1.  **阅读上下文**: 完整阅读本文件 (`AGENTS.md`)。
2.  **更新文档**: 
    - 每次完成重大功能开发或里程碑后，必须同步更新 `AGENTS.md`。
    - 每次修改项目技术栈时，必须更新 `AGENTS.md`。
3.  **Git 操作**:
    - 除非用户明确要求，禁止执行任何 Git 提交 (`git commit`) 或推送 (`git push`) 操作。
    - 在执行 Git 操作前，若 `AGENTS.md` 已有更新，必须确保该文件已被包含在提交中。

## 当前状态与里程碑

- [x] **项目定义与架构设计** (2026-03-05)
- [x] **阶段 1: 核心爬虫与 OCR 引擎开发** (2026-03-05)
    - [x] 实现 `SimpleOCR` 像素采样算法
    - [x] 实现 `ZimukuAgent` 搜索逻辑与验证码处理
    - [x] 实现详情页下载链接提取与文件下载功能
    - [x] 实现文件名乱码修复 (CP437 -> GBK)

- [x] **阶段 2: 数据库与 FastAPI 基础架构** (2026-03-06)
    - [x] 使用 `SQLModel` 定义数据库模型
    - [x] 初始化 FastAPI 框架并添加全局异常处理
    - [x] 开发配置管理 API
    - [x] 实现带 SQLite 缓存的搜索 API
    - [x] 完成数据库 CRUD 与 API 的单元测试
- [x] **阶段 3: 下载服务与 MCP 协议集成** (2026-03-06)
    - [x] 开发下载路由并集成后台下载任务
    - [x] 实现 `ArchiveManager` 支持 ZIP/7z 自动解压与乱码修复
    - [x] 集成 MCP SDK，暴露搜索与下载为 MCP Tools
    - [x] 完成解压编码修复逻辑的单元测试
- [x] **阶段 3.5: Web UI 增强型 API 开发** (2026-03-06)
    - [x] 实现任务管理 API (列表、删除、重试、清理)
    - [x] 实现媒体库管理 API (路径增删改查)
    - [x] 实现自动化匹配 API 骨架 (/media/match)
    - [x] 实现系统监控 API (存储统计、任务统计、日志查看)
    - [x] 完成增强型 API 的单元测试 (覆盖率 100% 核心端点)
    - [x] 通过 Ruff 代码规范检查与格式化
- [ ] **阶段 4: Web UI 面板开发** (进行中)
    - [x] 完成 UI 视觉设计 (使用 Pencil 绘制 5 个核心页面：搜索、电影、剧集、任务、设置)
    - [ ] 初始化前端项目 (Vite + React)
- [ ] **阶段 5: 容器化与文档交付**

## 技术决策记录 (TDR)

- **2026-03-05**: 初始化项目。确认为独立 `zimuku-subtitle-server`。
- **2026-03-05**: **[架构决策]** 采用双 OCR 策略：默认使用轻量级 `SimpleOCR`（参考参考代码逻辑），可选 `DdddOCR` 作为后备方案。
- **2026-03-05**: **[关键逻辑]** 确认实现 ZIP 压缩包文件名乱码修复功能（`CP437` -> `GBK`）。
- **2026-03-05**: **[集成方案]** 决定原生支持 MCP 协议，方便 AI 驱动的自动化字幕管理。
- **2026-03-05**: **[开发规范]** 强制要求在 `.venv` 虚拟环境中进行 Python 开发，以确保依赖隔离和环境一致性。
- **2026-03-06**: **[UI 设计]** 确定采用极简现代化风格，侧边栏导航 + 主内容区模式。核心功能涵盖：极简搜索（带语言筛选）、媒体目录关联、剧集批量下载与自动重命名。
