---
phase: 04-zi-mu-xia-zai-ding-wei-yi-dong
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - app/db/models.py
  - app/api/tasks.py
  - app/services/task_service.py
  - frontend/src/api.ts
autonomous: true
requirements:
  - DOWN-04

must_haves:
  truths:
    - Backend API accepts target_path, target_type, season, episode, language parameters
    - Download task saves target info to database
    - After successful download, file is moved to target directory with renamed filename (video_name.语言.ext)
    - On move failure, file stays in download directory with error message
  artifacts:
    - path: app/db/models.py
      provides: SubtitleTask model extended with target fields
      contains: target_path, target_type, season, episode, language
    - path: app/api/tasks.py
      provides: create_download_task accepts new parameters
      exports: target_path, target_type, season, episode, language
    - path: app/services/task_service.py
      provides: File move logic after download completes
      contains: shutil.move, error handling, backup retention
    - path: frontend/src/api.ts
      provides: createDownloadTask accepts target parameters
      exports: target_path, target_type, season, episode, language
  key_links:
    - from: tasks.py
      to: task_service.py
      via: TaskService.create_task
      pattern: target_path=target_path
    - from: task_service.py
      to: models.py
      via: SubtitleTask
      pattern: target_path
---

<objective>
Extend backend API to accept target path parameters and implement file move/rename logic after download completes.

Purpose: Per locked decisions - enable two-level target selection, implement immediate file move after download, rename to include language tag, keep backup on failure.
Output: Extended SubtitleTask model, updated API, file move logic in TaskService
</objective>

<execution_context>
@/home/cy/.claude/get-shit-done/workflows/execute-plan.md
@/home/cy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-zi-mu-xia-zai-ding-wei-yi-dong/04-CONTEXT.md
@.planning/phases/04-zi-mu-xia-zai-ding-wei-yi-dong/04-RESEARCH.md
@app/db/models.py
@app/api/tasks.py
@app/services/task_service.py
@frontend/src/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SubtitleTask model with target fields</name>
  <files>app/db/models.py</files>
  <action>
    In SubtitleTask model, add new fields:
    - target_path: Optional[str] = None  # Target directory for moved file
    - target_type: Optional[str] = None  # "movie" or "tv"
    - season: Optional[int] = None       # For TV series
    - episode: Optional[int] = None      # For TV series
    - language: Optional[str] = None      # For filename (简体/繁体/etc)
  </action>
  <verify>
    <automated>grep -n "target_path" app/db/models.py</automated>
  </verify>
  <done>SubtitleTask model has target_path, target_type, season, episode, language fields</done>
</task>

<task type="auto">
  <name>Task 2: Update tasks API endpoint</name>
  <files>app/api/tasks.py</files>
  <action>
    Update create_download_task endpoint:
    - Add parameters: target_path, target_type, season, episode, language (all Optional)
    - Pass all parameters to TaskService.create_task
    - Keep backward compatibility: if no target_path, use None
  </action>
  <verify>
    <automated>grep -n "target_path" app/api/tasks.py</automated>
  </verify>
  <done>tasks.py API accepts target parameters</done>
</task>

<task type="auto">
  <name>Task 3: Update TaskService.create_task</name>
  <files>app/services/task_service.py</files>
  <action>
    Update TaskService.create_task:
    - Add parameters: target_path, target_type, season, episode, language
    - Pass to SubtitleTask constructor
  </action>
  <verify>
    <automated>grep -n "def create_task" app/services/task_service.py</automated>
  </verify>
  <done>TaskService.create_task accepts target parameters</done>
</task>

<task type="auto">
  <name>Task 4: Add file move logic in run_download_task</name>
  <files>app/services/task_service.py</files>
  <action>
    In run_download_task, after successful download (after line ~128):
    - If task.target_path exists:
      - Get video filename: use target_type, season, episode to find matching file in target_path
      - Get extension from downloaded file
      - Format new filename: f"{video_filename}.{task.language}{ext}"
      - target_full_path = os.path.join(task.target_path, new_filename)
      - Try: shutil.move(task.save_path, target_full_path)
        - Update task.save_path to new path
      - Catch Exception:
        - Log error
        - Set task.error_msg = f"下载成功但移动失败: {e}"
        - Keep file in original download directory (backup)
  </action>
  <verify>
    <automated>grep -n "shutil.move" app/services/task_service.py</automated>
  </verify>
  <done>File move logic implemented with error handling and backup retention</done>
</task>

<task type="auto">
  <name>Task 5: Update frontend API</name>
  <files>frontend/src/api.ts</files>
  <action>
    Update createDownloadTask function:
    - Add parameters: target_path?, target_type?, season?, episode?, language?
    - Pass all parameters to the API endpoint via params
  </action>
  <verify>
    <automated>grep -n "target_path" frontend/src/api.ts</automated>
  </verify>
  <done>Frontend API passes target parameters to backend</done>
</task>

</tasks>

<verification>
- [ ] SubtitleTask model has target fields
- [ ] tasks.py API accepts target parameters
- [ ] TaskService.create_task saves target info
- [ ] run_download_task moves file after download
- [ ] File renamed to include language tag
- [ ] On failure, file stays in download directory
</verification>

<success_criteria>
Backend accepts target path parameters. After download completes, file is moved to target directory with language-tagged filename. On failure, backup is retained.
</success_criteria>

<output>
After completion, create `.planning/phases/04-zi-mu-xia-zai-ding-wei-yi-dong/04-03-SUMMARY.md`
</output>
