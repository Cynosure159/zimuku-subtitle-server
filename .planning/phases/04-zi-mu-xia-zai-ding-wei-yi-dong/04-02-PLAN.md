---
phase: 04-zi-mu-xia-zai-ding-wei-yi-dong
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Modal.tsx
  - frontend/src/components/MediaSelector.tsx
  - frontend/src/components/EpisodeSelector.tsx
  - frontend/src/components/DownloadModal.tsx
autonomous: true
requirements:
  - DOWN-03

must_haves:
  truths:
    - Modal uses white background with gray border (no glassmorphism)
    - Download modal has two-level media selector (movie/series then season/episode)
    - User can select target media and specify season/episode for TV shows
    - User can expand advanced options for custom path input
  artifacts:
    - path: frontend/src/components/Modal.tsx
      provides: Modal with white background and gray border, no glassmorphism
      min_lines: 50
    - path: frontend/src/components/MediaSelector.tsx
      provides: Two-level movie/series selection component
      min_lines: 80
    - path: frontend/src/components/EpisodeSelector.tsx
      provides: Season → episode selection for TV shows
      min_lines: 50
    - path: frontend/src/components/DownloadModal.tsx
      provides: Updated to use MediaSelector and new modal style
      min_lines: 100
  key_links:
    - from: DownloadModal.tsx
      to: MediaSelector.tsx
      via: import and render
      pattern: MediaSelector
    - from: DownloadModal.tsx
      to: Modal.tsx
      via: className
      pattern: border-slate-200.*rounded-xl
---

<objective>
Restyle modal to use white background with gray border, create media selector for two-level movie/series selection, and integrate into DownloadModal.

Purpose: Per locked decisions - remove glassmorphism, implement two-level selection (movie/series then season/episode).
Output: Updated Modal, MediaSelector, EpisodeSelector, DownloadModal
</objective>

<execution_context>
@/home/cy/.claude/get-shit-done/workflows/execute-plan.md
@/home/cy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-zi-mu-xia-zai-ding-wei-yi-dong/04-CONTEXT.md
@.planning/phases/04-zi-mu-xia-zai-ding-wei-yi-dong/04-RESEARCH.md
@frontend/src/components/Modal.tsx
@frontend/src/components/DownloadModal.tsx
@frontend/src/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Modal component</name>
  <files>frontend/src/components/Modal.tsx</files>
  <action>
    Update Modal.tsx:
    - Remove backdrop-blur-sm from className
    - Add bg-white and border border-slate-200 to the dialog element
    - Keep rounded-xl (16px) for consistent styling with other components
    - Ensure overlay is bg-black/50 (semi-transparent black, no blur)
  </action>
  <verify>
    <automated>grep -n "backdrop-blur" frontend/src/components/Modal.tsx || echo "removed"</automated>
  </verify>
  <done>Modal uses white background with gray border, no glassmorphism</done>
</task>

<task type="auto">
  <name>Task 2: Create MediaSelector component</name>
  <files>frontend/src/components/MediaSelector.tsx</files>
  <action>
    Create MediaSelector component with:
    - State for selected type (movie/tv)
    - Two tabs/buttons to switch between movies and TV series
    - List display of media items from API (use listScannedFiles)
    - Each item shows: title, year (for movies), episode count (for series)
    - For series, clicking expands to show seasons
    - Returns selected media info: { id, title, type, path, year?, episode_count? }
    - Props: onSelect (callback)
  </action>
  <verify>
    <automated>ls -la frontend/src/components/MediaSelector.tsx</automated>
  </verify>
  <done>MediaSelector component created with two-level selection</done>
</task>

<task type="auto">
  <name>Task 3: Create EpisodeSelector component</name>
  <files>frontend/src/components/EpisodeSelector.tsx</files>
  <action>
    Create EpisodeSelector component with:
    - Props: seriesTitle, onSelect (callback)
    - Fetch available episodes/seasons from API (use matchTVSeason or similar)
    - Display seasons as buttons
    - When season selected, display episodes as a grid of buttons
    - Returns: { season, episode }
  </action>
  <verify>
    <automated>ls -la frontend/src/components/EpisodeSelector.tsx</automated>
  </verify>
  <done>EpisodeSelector component created for season → episode selection</done>
</task>

<task type="auto">
  <name>Task 4: Update DownloadModal with new components</name>
  <files>frontend/src/components/DownloadModal.tsx</files>
  <action>
    Update DownloadModal.tsx:
    - Replace PathSelector with MediaSelector
    - Add "Advanced Options" toggle that reveals manual path input
    - Pass target info to createDownloadTask: { target_path, target_type, season, episode, language }
    - Maintain language and format selection UI
    - Show selected target display (e.g., "Movie: Inception (2010)" or "TV: Breaking Bad S01E01")
  </action>
  <verify>
  <automated>grep -n "MediaSelector" frontend/src/components/DownloadModal.tsx</automated>
  </verify>
  <done>DownloadModal updated with MediaSelector and advanced options</done>
</task>

</tasks>

<verification>
- [ ] Modal.tsx no longer has backdrop-blur-sm
- [ ] Modal has white background with border-slate-200
- [ ] MediaSelector provides two-level selection (movie/series)
- [ ] EpisodeSelector provides season → episode selection
- [ ] DownloadModal integrates both selectors
</verification>

<success_criteria>
Modal styled with white background and gray border. DownloadModal includes two-level media selector with advanced path options.
</success_criteria>

<output>
After completion, create `.planning/phases/04-zi-mu-xia-zai-ding-wei-yi-dong/04-02-SUMMARY.md`
</output>
