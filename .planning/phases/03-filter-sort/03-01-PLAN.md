---
phase: 03-filter-sort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/MediaFilterBar.tsx
  - frontend/src/pages/MoviesPage.tsx
  - frontend/src/pages/SeriesPage.tsx
autonomous: true
requirements:
  - FS-01
  - FS-02
  - FS-03
must_haves:
  truths:
    - User can filter movies/series by "has subtitles" or "missing subtitles"
    - User can sort lists by name/year/created/subtitle_status
    - UI shows filter controls at top of media list with side-by-side layout
    - Empty filter results show message with clear button
  artifacts:
    - path: frontend/src/components/MediaFilterBar.tsx
      provides: Reusable filter/sort dropdown component
    - path: frontend/src/pages/MoviesPage.tsx
      provides: Filter and sort state with useMemo filtering
    - path: frontend/src/pages/SeriesPage.tsx
      provides: Filter and sort state with useMemo filtering
  key_links:
    - from: MoviesPage.tsx
      to: MediaFilterBar.tsx
      via: Component import and usage
    - from: SeriesPage.tsx
      to: MediaFilterBar.tsx
      via: Component import and usage
    - from: MediaFilterBar.tsx
      to: sidebarItems
      via: counts prop passed from parent
---

<objective>
Implement filter and sort functionality for media library pages (Movies and Series). Users can filter by subtitle status (all/has/missing) with counts, and sort by name/year/created/subtitle status with toggle direction.
</objective>

<execution_context>
@/home/cy/.claude/get-shit-done/workflows/execute-plan.md
@/home/cy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-filter-sort/03-CONTEXT.md
@.planning/phases/03-filter-sort/03-RESEARCH.md
@.planning/phases/03-filter-sort/03-VALIDATION.md

# Existing code patterns

## Current MoviesPage filtering (line 24-37)
```typescript
const groupedMovies = useMemo(() => {
  // ... groups files by title
  return Object.values(groups).filter(m =>
    m.title.toLowerCase().includes(searchTerm.toLowerCase())
  ).sort((a, b) => a.title.localeCompare(b.title));
}, [files, searchTerm]);
```

## SidebarItem structure (from MediaSidebar)
```typescript
interface SidebarItem {
  id: string;
  displayTitle: string;
  year?: string;
  totalCount: number;  // total files
  hasSubCount: number; // files with subtitles
  poster?: string | null;
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MediaFilterBar component</name>
  <files>frontend/src/components/MediaFilterBar.tsx</files>
  <action>
Create a reusable MediaFilterBar component that provides:
- Filter dropdown with 3 options: 全部 (All), 有字幕 (Has), 无字幕 (Missing)
- Each option shows count in format "有字幕 (80)"
- Sort dropdown with 4 options: 名称, 年份, 添加时间, 字幕状态
- Clicking same sort option toggles direction (asc/desc)
- Use Tailwind CSS for styling, place dropdowns side by side
- Component accepts: filter, setFilter, sortBy, setSortBy, sortDesc, setSortDesc, counts

The component should follow the user's locked decisions:
- Controls at top of media list
- Filter and sort side by side (left-right split)
- Filter applies immediately on selection
- Default sort: 名称正序 (A-Z)
  </action>
  <verify>
    <automated>ls -la frontend/src/components/MediaFilterBar.tsx</automated>
  </verify>
  <done>MediaFilterBar.tsx created with proper types and component exports</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add filter/sort state to MoviesPage</name>
  <files>frontend/src/pages/MoviesPage.tsx</files>
  <action>
Add filter and sort functionality to MoviesPage:

1. Import MediaFilterBar component
2. Add state for filter (all/has/missing), sortBy, sortDesc
3. Calculate filter counts from sidebarItems: all, has, missing
4. Modify groupedMovies useMemo to:
   - First apply filter (has_subtitle status)
   - Then apply sort based on sortBy/sortDesc
   - Sort options: name (localeCompare), year (localeCompare), created (by first file's created_at), subtitle_status (ratio hasSubCount/totalCount)
5. Add MediaFilterBar component above MediaSidebar
6. Add empty state with clear filter button when filtered results are empty

Follow the user's locked decisions:
- Filter options with counts: 全部 (all), 有字幕 (totalCount === hasSubCount), 无字幕 (hasSubCount < totalCount)
- Default sort: name ascending
- Toggle sort direction by clicking same option
  </action>
  <verify>
    <automated>grep -n "filter\|sort\|MediaFilterBar" frontend/src/pages/MoviesPage.tsx | head -20</automated>
  </verify>
  <done>MoviesPage has filter dropdown with counts, sort dropdown with toggle, empty state with clear button</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add filter/sort state to SeriesPage</name>
  <files>frontend/src/pages/SeriesPage.tsx</files>
  <action>
Add filter and sort functionality to SeriesPage following the same pattern as MoviesPage:

1. Import MediaFilterBar component
2. Add state for filter, sortBy, sortDesc
3. Calculate filter counts from sidebarItems
4. Modify groupedSeries useMemo to apply filter then sort
5. Add MediaFilterBar component above MediaSidebar
6. Add empty state with clear filter button

Ensure SeriesPage handles season-level filtering correctly:
- A series "has subtitles" if ALL its episodes have subtitles (totalCount === hasSubCount)
- A series "missing subtitles" if ANY episode is missing (hasSubCount < totalCount)
  </action>
  <verify>
    <automated>grep -n "filter\|sort\|MediaFilterBar" frontend/src/pages/SeriesPage.tsx | head -20</automated>
  </verify>
  <done>SeriesPage has identical filter/sort functionality as MoviesPage</done>
</task>

</tasks>

<verification>
- [ ] Filter dropdown shows 3 options with correct counts
- [ ] Selecting filter immediately updates the list
- [ ] Sort dropdown shows 4 options
- [ ] Clicking same sort option toggles direction (arrow indicator)
- [ ] Empty state shows when filter results in no items
- [ ] Clear filter button resets filter to "all"
</verification>

<success_criteria>
User can filter movies/series by "has subtitles" or "missing subtitles" - visible in list changes
User can sort lists by name/year/created/subtitle_status - visible in list order changes
UI shows filter controls at top of media list with side-by-side layout
Empty filter results show message with clear button
</success_criteria>

<output>
After completion, create `.planning/phases/03-filter-sort/03-01-SUMMARY.md`
</output>
