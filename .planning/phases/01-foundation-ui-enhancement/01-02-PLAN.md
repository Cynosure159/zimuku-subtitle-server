---
phase: 01-foundation-ui-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/media.py
  - app/core/metadata.py
  - app/services/__init__.py
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03

must_haves:
  truths:
    - "User can view video metadata (title, year, plot, rating) extracted from NFO files"
    - "User can see poster images extracted from local folder (folder.jpg, poster.jpg)"
    - "User can view fallback metadata from TXT files when NFO is unavailable"
  artifacts:
    - path: "app/core/metadata.py"
      provides: "Metadata extraction functions (parse_nfo, find_poster, parse_txt_info)"
    - path: "app/api/media.py"
      provides: "Metadata API endpoints"
  key_links:
    - from: "app/api/media.py"
      to: "app/core/metadata.py"
      via: "imports parse_nfo, find_poster, parse_txt_info"
---

<objective>
Implement backend metadata extraction: NFO parsing, poster detection, and TXT fallback.

Purpose: Extract video metadata (title, year, plot, rating) from NFO files, detect poster images in local folders, parse TXT files as fallback.

Output: New metadata module, API endpoint for metadata retrieval.
</objective>

<execution_context>
@/home/cy/.claude/get-shit-done/workflows/execute-plan.md
@/home/cy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/cy/project/zimuku-subtitle-server/app/api/media.py

From RESEARCH.md:
- Use Python xml.etree.ElementTree for NFO parsing
- Support XBMC/Kodi NFO format
- Poster detection: folder.jpg, poster.jpg, poster.png, folder.png, same-name poster
- Try multiple encodings for NFO (UTF-8, GBK, GB2312)
- Simple key:value format for TXT fallback

Pattern to follow:
- parse_nfo(nfo_path: Path) -> dict
- find_poster(folder: Path) -> Optional[Path]
- parse_txt_info(txt_path: Path) -> Optional[dict]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create metadata extraction module</name>
<files>app/core/metadata.py</files>
<action>
Create app/core/metadata.py with:
1. parse_nfo(nfo_path: Path) -> dict:
   - Use xml.etree.ElementTree
   - Handle ParseError gracefully
   - Try encodings: UTF-8, GBK, GB2312
   - Extract: title, year, plot, rating, genres, director
2. find_poster(folder: Path) -> Optional[Path]:
   - Check: folder.jpg, poster.jpg, poster.png, folder.png
   - Also check for same-name poster (video.mp4 -> video.jpg)
   - Return first match
3. parse_txt_info(txt_path: Path) -> Optional[dict]:
   - Simple key:value parsing
   - Support: title, year, plot, description
   - Try UTF-8 encoding

Use pathlib for path operations.
</action>
<verify>
<automated>ls app/core/metadata.py</automated>
</verify>
<done>Metadata extraction module with NFO, poster, TXT parsing functions</done>
</task>

<task type="auto">
  <name>Task 2: Add metadata API endpoint</name>
<files>app/api/media.py</files>
<action>
Add to app/api/media.py:
1. Import metadata module functions
2. Add GET /media/metadata/{file_id} endpoint:
   - Accept file_id as path parameter
   - Look up ScannedFile in database
   - Get folder path from file_path
   - Call parse_nfo, find_poster, parse_txt_info
   - Return: {title, year, plot, rating, genres, poster_path, nfo_data, txt_info}
   - Handle file not found with 404

For poster_path, return relative path from media root for frontend to construct URL.
</action>
<verify>
<automated>grep -l "metadata" app/api/media.py</automated>
</verify>
<done>API endpoint returns metadata for any media file ID</done>
</task>

<task type="auto">
  <name>Task 3: Add poster image serving endpoint</name>
<files>app/api/media.py</files>
<action>
Add to app/api/media.py:
1. Add GET /media/poster endpoint:
   - Accept path query parameter (URL-encoded relative path)
   - Use FileResponse to serve image
   - Handle file not found with 404
   - Set appropriate Content-Type based on extension

This enables frontend to display poster images.
</action>
<verify>
<automated>grep -l "poster" app/api/media.py</automated>
</verify>
<done>Poster images can be served via API</done>
</task>

</tasks>

<verification>
- metadata.py created with all parsing functions
- API endpoint returns NFO, poster, TXT data for any file_id
- Poster serving endpoint works for valid paths
- Run pytest to verify no regressions
</verification>

<success_criteria>
- User can view video metadata (title, year, plot, rating) extracted from NFO
- User can see poster images from local folder
- User can view fallback metadata from TXT files when NFO unavailable
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ui-enhancement/01-02-SUMMARY.md`
</output>
