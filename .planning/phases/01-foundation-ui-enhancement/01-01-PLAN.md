---
phase: 01-foundation-ui-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/main.tsx
  - frontend/src/hooks/useMediaQueries.ts
  - frontend/src/hooks/useTaskQueries.ts
  - frontend/src/hooks/useSearchQueries.ts
  - frontend/src/lib/queryClient.ts
  - frontend/src/stores/useUIStore.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "Application uses TanStack Query for efficient server state management"
    - "Application uses Zustand for UI state (sidebar, theme)"
  artifacts:
    - path: "frontend/src/lib/queryClient.ts"
      provides: "QueryClient configuration with 5-min cache"
    - path: "frontend/src/hooks/useMediaQueries.ts"
      provides: "TanStack Query hooks for media data"
    - path: "frontend/src/hooks/useTaskQueries.ts"
      provides: "TanStack Query hooks for tasks"
    - path: "frontend/src/hooks/useSearchQueries.ts"
      provides: "TanStack Query hooks for search"
    - path: "frontend/src/stores/useUIStore.ts"
      provides: "Zustand store for UI state"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "QueryClientProvider"
      via: "wraps application with query context"
    - from: "useMediaQueries.ts"
      to: "queryClient.ts"
      via: "uses useQuery/useMutation"
---

<objective>
Replace manual polling with TanStack Query and add Zustand for UI state management.

Purpose: Modernize data fetching with automatic caching, stale-while-revalidate, background refetch. Replace useState/global state with Zustand for sidebar and theme.

Output: TanStack Query hooks for media/tasks/search, Zustand store for UI state, QueryClient configuration.
</objective>

<execution_context>
@/home/cy/.claude/get-shit-done/workflows/execute-plan.md
@/home/cy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/cy/project/zimuku-subtitle-server/frontend/src/api.ts
@/home/cy/project/zimuku-subtitle-server/frontend/src/hooks/useMediaPolling.ts

From CONTEXT.md (locked decisions):
- TanStack Query for all API calls
- Cache duration: 5 minutes with stale-while-revalidate
- Background refetch on window focus and network reconnect
- Loading states: Skeleton placeholders matching component shapes
- Error handling: Toast notifications with retry option
- Optimistic updates for mutations (immediate UI update, rollback on error)

From RESEARCH.md:
- Use @tanstack/react-query v5
- Use zustand v5
- Configure QueryClient with staleTime: 5 * 60 * 1000
- refetchOnWindowFocus: true, refetchOnReconnect: true
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up QueryClient and install dependencies</name>
  <files>frontend/src/lib/queryClient.ts, frontend/src/App.tsx</files>
  <action>
    1. Install dependencies: `cd frontend && npm install @tanstack/react-query zustand`
    2. Create QueryClient configuration in frontend/src/lib/queryClient.ts:
       - Create QueryClient with defaultOptions:
         - staleTime: 5 * 60 * 1000 (5 minutes)
         - refetchOnWindowFocus: true
         - refetchOnReconnect: true
         - retry: 1
    3. Wrap App component with QueryClientProvider in App.tsx
  </action>
  <verify>
    <automated>ls frontend/src/lib/queryClient.ts && grep -l "QueryClientProvider" frontend/src/App.tsx</automated>
  </verify>
  <done>QueryClient configured with 5-min cache, App wrapped with provider</done>
</task>

<task type="auto">
  <name>Task 2: Create TanStack Query hooks for media data</name>
  <files>frontend/src/hooks/useMediaQueries.ts</files>
  <action>
    Create frontend/src/hooks/useMediaQueries.ts with:
    - useMediaPaths(): Fetch media paths using useQuery
    - useScannedFiles(type): Fetch scanned files using useQuery
    - useMediaStatus(): Fetch media scan status with conditional polling
      - Polling enabled when is_scanning or matching_files/seasons exist
      - 2000ms interval when active, false when idle
    - useMediaMutation(): Use useMutation for adding/removing paths
    Query keys: ['media', 'paths'], ['media', 'files', type], ['media', 'status']
  </action>
  <verify>
    <automated>ls frontend/src/hooks/useMediaQueries.ts</automated>
  </verify>
  <done>TanStack Query hooks replace useMediaPolling for media data</done>
</task>

<task type="auto">
  <name>Task 3: Create TanStack Query hooks for tasks and search</name>
  <files>frontend/src/hooks/useTaskQueries.ts, frontend/src/hooks/useSearchQueries.ts</files>
  <action>
    Create frontend/src/hooks/useTaskQueries.ts:
    - useTasks(): Fetch all tasks
    - useTaskStats(): Fetch task statistics
    - useCreateTask(): useMutation for creating tasks
    - useRetryTask(), useDeleteTask(): useMutation for task operations

    Create frontend/src/hooks/useSearchQueries.ts:
    - useSearch(query): Fetch search results with cache
    - useSearchCache(): Fetch search cache entries
    Query keys: ['tasks'], ['tasks', 'stats'], ['search', query]
  </action>
  <verify>
    <automated>ls frontend/src/hooks/useTaskQueries.ts frontend/src/hooks/useSearchQueries.ts</automated>
  </verify>
  <done>TanStack Query hooks for tasks and search implemented</done>
</task>

<task type="auto">
  <name>Task 4: Create Zustand store for UI state</name>
  <files>frontend/src/stores/useUIStore.ts</files>
  <action>
    Create frontend/src/stores/useUIStore.ts with:
    - Interface UIState:
      - sidebarOpen: boolean
      - theme: 'light' | 'dark'
      - selectedMediaId: number | null
      - actions: toggleSidebar, setTheme, setSelectedMediaId
    - Create store using zustand create<UIState>
    - Default: sidebarOpen: true, theme: 'light', selectedMediaId: null

    Note: Only create the store - integration with components happens in Plan 03
  </action>
  <verify>
    <automated>ls frontend/src/stores/useUIStore.ts</automated>
  </verify>
  <done>Zustand store created for sidebar, theme, selectedMediaId</done>
</task>

</tasks>

<verification>
- QueryClient configured with correct defaults
- TanStack Query hooks created for media, tasks, search
- Zustand store created with proper TypeScript types
- No existing tests broken (run pytest to verify backend)
</verification>

<success_criteria>
- TanStack Query replaces manual polling with 5-min cache
- Background refetch enabled on window focus and network reconnect
- Zustand store provides global UI state without Context providers
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ui-enhancement/01-01-SUMMARY.md`
</output>
