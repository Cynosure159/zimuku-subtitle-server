---
phase: 01-foundation-ui-enhancement
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - frontend/src/components/MediaInfoCard.tsx
  - frontend/src/components/MediaSidebar.tsx
  - frontend/src/pages/MoviesPage.tsx
  - frontend/src/pages/SeriesPage.tsx
  - frontend/src/pages/TasksPage.tsx
  - frontend/src/hooks/useMetadata.ts
autonomous: true
requirements:
  - META-04
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "User sees poster images extracted from local folder"
    - "User sees video metadata (title, year, plot, rating) displayed in frontend"
    - "User experiences smooth UI with transitions and hover effects"
    - "User sees responsive layout adapting to different screen sizes"
  artifacts:
    - path: "frontend/src/hooks/useMetadata.ts"
      provides: "Metadata query hook for frontend"
    - path: "frontend/src/components/MediaInfoCard.tsx"
      provides: "Poster and metadata display component"
    - path: "frontend/src/components/MediaSidebar.tsx"
      provides: "Responsive sidebar with Zustand state"
  key_links:
    - from: "MediaInfoCard.tsx"
      to: "useMetadata.ts"
      via: "fetches metadata from API"
    - from: "MediaSidebar.tsx"
      to: "useUIStore.ts"
      via: "connects to Zustand store for sidebar state"
---

<objective>
Implement frontend UI enhancements: display video poster/info cards, improve visual aesthetics, add transitions/hover effects, and implement responsive design.

Purpose: Show rich media information with improved visual experience. Integrate metadata from backend API, apply modern Tailwind patterns, and ensure responsive layout.

Output: Enhanced MediaInfoCard with poster display, responsive layout with sidebar behavior, smooth transitions.
</objective>

<execution_context>
@/home/cy/.claude/get-shit-done/workflows/execute-plan.md
@/home/cy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/cy/project/zimuku-subtitle-server/frontend/src/components/MediaInfoCard.tsx
@/home/cy/project/zimuku-subtitle-server/frontend/src/components/MediaSidebar.tsx

From CONTEXT.md (user deferred to planner):
- Visual Style: Colors, typography, spacing, card shadows (follow modern Tailwind patterns)
- Metadata Display: Poster image sources, fallback hierarchy, card layout
- Responsive Layout: Breakpoints, mobile navigation, sidebar behavior

From RESEARCH.md:
- Tailwind v4 CSS-first config
- Transition utilities, motion patterns
- Mobile-first responsive design
- Skeleton loaders for loading states
- Toast notifications for errors (using sonner)

From Plan 01 (parallel): Zustand store will be available at frontend/src/stores/useUIStore.ts
From Plan 02 (parallel): Metadata API available at GET /media/metadata/{file_id}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create metadata query hook</name>
<files>frontend/src/hooks/useMetadata.ts</files>
<action>
Create frontend/src/hooks/useMetadata.ts:
- useMediaMetadata(fileId): Use useQuery to fetch metadata from /media/metadata/{fileId}
- Query key: ['media', 'metadata', fileId]
- Include poster_url in response construction (call /media/poster endpoint)
- Handle loading (skeleton), error (toast with retry)

This hook will be used by MediaInfoCard to fetch and display metadata.
</action>
<verify>
<automated>ls frontend/src/hooks/useMetadata.ts</automated>
</verify>
<done>Metadata query hook created for fetching poster and info</done>
</task>

<task type="auto">
  <name>Task 2: Enhance MediaInfoCard with poster and metadata display</name>
<files>frontend/src/components/MediaInfoCard.tsx</files>
<action>
Update MediaInfoCard.tsx:
1. Use useMediaMetadata hook to fetch metadata
2. Display poster image with fallback hierarchy:
   - Use /media/poster?path={relative_path} for poster_url
   - Fallback: gray box with ImageIcon (existing behavior)
3. Display metadata: title, year, plot, rating, genres
4. Add skeleton loader matching component shape during loading
5. Add toast notification on error with retry button

Apply modern Tailwind patterns:
- Use rounded-xl, shadow-sm, hover:shadow-md transitions
- Use semantic colors (slate, blue accent)
- Use flexbox/grid for layout
</action>
<verify>
<automated>grep -l "useMediaMetadata" frontend/src/components/MediaInfoCard.tsx</automated>
</done>
<done>MediaInfoCard displays poster and metadata from API</done>
</task>

<task type="auto">
  <name>Task 3: Implement responsive layout with Zustand sidebar</name>
<files>frontend/src/components/MediaSidebar.tsx, frontend/src/pages/MoviesPage.tsx, frontend/src/pages/SeriesPage.tsx</files>
<action>
1. Integrate useUIStore in MediaSidebar.tsx:
   - Use sidebarOpen state for visibility
   - Use toggleSidebar for hamburger menu

2. Update page layouts (MoviesPage, SeriesPage):
   - Mobile-first: flex-col, stacked layout
   - Desktop (lg:): flex-row, sidebar on left
   - Use hidden/lg:block for sidebar toggle

3. Add responsive breakpoints:
   - Mobile (<lg): hamburger menu, full-width content
   - Desktop (lg+): persistent sidebar option

Use Tailwind v4 responsive patterns: flex flex-col lg:flex-row
</action>
<verify>
<automated>grep -l "useUIStore" frontend/src/components/MediaSidebar.tsx</automated>
</done>
<done>Responsive layout with Zustand-controlled sidebar</done>
</task>

<task type="auto">
  <name>Task 4: Add transitions, hover effects, and visual polish</name>
<files>frontend/src/components/MediaListItem.tsx, frontend/src/pages/TasksPage.tsx</files>
<action>
1. Add transitions to MediaListItem:
   - transition-all duration-200
   - hover:bg-slate-50, hover:shadow-sm
   - Scale or translate on hover for feedback

2. Improve TasksPage visual:
   - Add transition on task status changes
   - Hover effects on action buttons
   - Skeleton loaders for task lists

3. Apply visual polish:
   - Consistent rounded-xl/rounded-2xl
   - Better spacing (gap-3, gap-4)
   - Subtle shadows (shadow-sm)
   - Focus rings for accessibility

Follow modern Tailwind patterns from RESEARCH.md.
</action>
<verify>
<automated>grep -l "transition" frontend/src/components/MediaListItem.tsx</automated>
</done>
<done>Smooth transitions and hover effects implemented</done>
</task>

</tasks>

<verification>
- MediaInfoCard shows poster and metadata from API
- Responsive layout works on mobile and desktop
- Transitions and hover effects visible on interactive elements
- No console errors, application loads correctly
</verification>

<success_criteria>
- User sees poster images extracted from local folder
- User sees video metadata (title, year, plot, rating)
- User experiences smooth UI with transitions and hover effects
- User sees responsive layout adapting to different screen sizes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ui-enhancement/01-03-SUMMARY.md`
</output>
