---
phase: 02-manual-download
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/scraper.py
  - app/services/search_service.py
autonomous: true
requirements:
  - DOWN-01
must_haves:
  truths:
    - "Backend returns format field in subtitle search results"
    - "Backend returns fps field in subtitle search results"
    - "API /search/ includes format and fps for each subtitle"
  artifacts:
    - path: "app/core/scraper.py"
      provides: "SubtitleResult class with format and fps fields"
      contains: "format: Optional[str]"
      contains: "fps: Optional[str]"
  key_links:
    - from: "app/core/scraper.py"
      to: "/search/ API"
      via: "SubtitleResult.to_dict()"
      pattern: "to_dict.*format.*fps"
---

<objective>
Extend backend to return format and FPS information in search results.

Purpose: Enable frontend to display detailed subtitle metadata (format, FPS) as required by DOWN-01.

Output: Backend returns format and fps fields in subtitle search results.
</objective>

<execution_context>
@/home/cy/.claude/get-shit-done/workflows/execute-plan.md
@/home/cy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/core/scraper.py
@app/services/search_service.py

# Current SubtitleResult structure (lines 38-63):
```python
class SubtitleResult:
    def __init__(self, title: str, link: str, lang: List[str], rating: str):
        self.title = title
        self.link = link
        self.lang = lang
        self.rating = rating

    def to_dict(self):
        return {
            "title": self.title,
            "link": self.link,
            "lang": self.lang,
            "rating": self.rating,
        }
```

# API search endpoint: /search/ calls search_service which returns SubtitleResult
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SubtitleResult class to include format and fps fields</name>
  <files>app/core/scraper.py</files>
  <action>
Add optional format and fps parameters to SubtitleResult.__init__:
- format: Optional[str] = None (e.g., "SRT", "ASS", "SSA", "SUB")
- fps: Optional[str] = None (e.g., "24fps", "25fps", "23.976fps")

Update to_dict() method to include these fields in the returned dictionary.
Update from_dict() classmethod to handle these new fields.
  </action>
  <verify>
<automated>python -c "from app.core.scraper import SubtitleResult; r = SubtitleResult('Test', 'http://x.com', ['中'], '5.0', format='SRT', fps='24fps'); print(r.to_dict())"</automated>
  </verify>
  <done>SubtitleResult.to_dict() returns format and fps fields</done>
</task>

<task type="auto">
  <name>Task 2: Enhance scraper to parse format/fps from Zimuku HTML</name>
  <files>app/core/scraper.py</files>
  <action>
Update the search result parsing logic in ZimukuAgent to extract format and fps:
- Parse format from subtitle entry (look for common patterns like SRT, ASS, SSA in the HTML)
- Extract FPS from the detail row (look for patterns like "24fps", "25fps")

If the data is not available in search results, fetch detail page and extract from there.
Pass format and fps when creating SubtitleResult objects.
  </action>
  <verify>
<automated>python -c "from app.core.scraper import ZimukuAgent; print('ZimukuAgent imported successfully')"</automated>
  </verify>
  <done>Search results include format and fps when available from Zimuku</done>
</task>

<task type="auto">
  <name>Task 3: Verify search API returns format/fps data</name>
  <files>app/api/search.py</files>
  <action>
Test the /search/ API endpoint returns format and fps fields in the response.

If there's a search_service.py, ensure it passes format/fps through to the API response.
  </action>
  <verify>
<automated>source .venv/bin/activate && ruff check app/core/scraper.py</automated>
  </verify>
  <done>API /search/ returns format and fps in each subtitle result</done>
</task>

</tasks>

<verification>
- SubtitleResult.to_dict() includes format and fps
- Scraping returns these fields in results
- API response contains format and fps for each subtitle
</verification>

<success_criteria>
User can view format and FPS information in search results (DOWN-01 requirement met).
Backend returns all metadata in single API call as per user decision.
</success_criteria>

<output>
After completion, create `.planning/phases/02-manual-download/02-01-SUMMARY.md`
</output>
