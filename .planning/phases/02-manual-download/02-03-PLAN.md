---
phase: 02-manual-download
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - frontend/src/components/Modal.tsx
  - frontend/src/components/DownloadModal.tsx
  - frontend/src/components/PathSelector.tsx
  - frontend/src/stores/useUIStore.ts
  - frontend/src/pages/SearchPage.tsx
  - frontend/src/api.ts
autonomous: true
requirements:
  - DOWN-03
  - DOWN-04
must_haves:
  truths:
    - "User can open modal to select language and format before download"
    - "User can specify custom download target path"
    - "Last used download path is remembered across sessions"
  artifacts:
    - path: "frontend/src/components/Modal.tsx"
      provides: "Reusable modal dialog component"
    - path: "frontend/src/components/DownloadModal.tsx"
      provides: "Download selection modal with language/format/path"
    - path: "frontend/src/components/PathSelector.tsx"
      provides: "Path selection with media paths and custom input"
  key_links:
    - from: "SearchPage.tsx"
      to: "DownloadModal"
      via: "onDownload button click"
      pattern: "setSelectedSubtitle.*modalOpen"
    - from: "PathSelector"
      to: "useUIStore"
      via: "setLastDownloadPath"
      pattern: "useUIStore.*lastDownloadPath"
---

<objective>
Implement download modal with language/format selection and custom download path selection.

Purpose: Allow users to select preferred language and format before downloading, and specify custom download target path.

Output: Download modal with language/format selection and path selector with memory.
</objective>

<execution_context>
@/home/cy/.claude/get-shit-done/workflows/execute-plan.md
@/home/cy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/SearchPage.tsx
@frontend/src/stores/useUIStore.ts
@frontend/src/api.ts

# Existing API endpoints:
# - GET /media/paths - list configured media paths
# - POST /tasks/ - create download task (already exists)

# From research, use existing patterns:
# - Modal pattern from research docs
# - PathSelector leveraging /media/paths API
# - Zustand for last download path memory
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Zustand store for download path memory</name>
  <files>frontend/src/stores/useUIStore.ts</files>
  <action>
Add to UIState:
- lastDownloadPath: string | null
- setLastDownloadPath: (path: string) => void

Initialize lastDownloadPath to null.
  </action>
  <verify>
<automated>cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Zustand store tracks lastDownloadPath</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable Modal component</name>
  <files>frontend/src/components/Modal.tsx</files>
  <action>
Create reusable Modal component:
- Accept isOpen, onClose, title, children props
- Use fixed overlay with backdrop blur
- Include close button (X icon)
- Support keyboard escape to close
- Use native dialog element or div with proper focus management

Export as default component.
  </action>
  <verify>
<automated>ls -la frontend/src/components/Modal.tsx</automated>
  </verify>
  <done>Modal component renders with backdrop and close functionality</done>
</task>

<task type="auto">
  <name>Task 3: Create PathSelector component</name>
  <files>frontend/src/components/PathSelector.tsx</files>
  <action>
Create PathSelector component:
- Fetch media paths from /media/paths API
- Display paths as clickable buttons
- Allow custom path input with text field
- Highlight last used path (from Zustand store)
- Call onSelect(path) callback when path is selected
- Save selection to Zustand store

Export as default component.
  </action>
  <verify>
<automated>ls -la frontend/src/components/PathSelector.tsx</automated>
  </verify>
  <done>PathSelector shows media paths and allows custom input</done>
</task>

<task type="auto">
  <name>Task 4: Create DownloadModal with language/format selection</name>
<files>frontend/src/components/DownloadModal.tsx</files>
  <action>
Create DownloadModal component:
- Accept isOpen, onClose, subtitle (SearchResult), onDownload callback
- Show subtitle title
- Display language options (from subtitle.langs) - allow multiple selection
- Display format option (from subtitle.format) - single selection
- Include PathSelector for download path
- Download button triggers onDownload with selected language, format, path
- Handle loading state during download

Export as default component.
  </action>
  <verify>
<automated>ls -la frontend/src/components/DownloadModal.tsx</automated>
  </verify>
  <done>DownloadModal renders with language/format selection and path selector</done>
</task>

<task type="auto">
  <name>Task 5: Integrate DownloadModal into SearchPage</name>
  <files>frontend/src/pages/SearchPage.tsx</files>
  <action>
Update SearchPage:
- Import DownloadModal component
- Add state: selectedSubtitle (SearchResult | null), modalOpen (boolean)
- Change download button to open modal instead of direct download
- In modal's onDownload: call createDownloadTask with selected options, close modal, show success
- Ensure modal passes all required data

Update API to support download with custom path:
- Modify createDownloadTask to accept optional target_path parameter
  </action>
  <verify>
<automated>cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>SearchPage opens DownloadModal when user clicks download</done>
</task>

</tasks>

<verification>
- Modal opens when download button clicked
- Language and format selection works
- Path selector shows media paths
- Custom path input works
- Download task created with selected options
</verification>

<success_criteria>
User can open modal to select preferred language and format before download (DOWN-03).
User can specify custom download target path for each subtitle (DOWN-04).
Last used path is remembered across sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-manual-download/02-03-SUMMARY.md`
</output>
