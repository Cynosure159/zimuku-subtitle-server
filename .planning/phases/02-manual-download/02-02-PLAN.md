---
phase: 02-manual-download
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/src/components/SearchResultRow.tsx
  - frontend/src/components/SearchResultDetails.tsx
  - frontend/src/pages/SearchPage.tsx
  - frontend/src/api.ts
autonomous: true
requirements:
  - DOWN-01
  - DOWN-02
must_haves:
  truths:
    - "User can view format and FPS information in search results"
    - "User can expand search results to see subtitle details"
    - "Expanded rows show smooth animation"
    - "Accordion behavior: clicking one row closes others"
  artifacts:
    - path: "frontend/src/components/SearchResultRow.tsx"
      provides: "Expandable row component with accordion"
    - path: "frontend/src/components/SearchResultDetails.tsx"
      provides: "Detailed subtitle info display (format, fps, langs, author)"
  key_links:
    - from: "SearchPage.tsx"
      to: "SearchResultRow"
      via: "import and render in map"
      pattern: "SearchResultRow.*item.*expandedIndex"
---

<objective>
Implement expandable search results with accordion behavior and display format/fps information.

Purpose: Allow users to view detailed subtitle information (format, FPS, author, download count) by clicking on search results.

Output: Search results display as expandable rows with smooth accordion animation.
</objective>

<execution_context>
@/home/cy/.claude/get-shit-done/workflows/execute-plan.md
@/home/cy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/core/scraper.py
@frontend/src/pages/SearchPage.tsx
@frontend/src/api.ts

# Current SearchResult interface (SearchPage.tsx lines 5-11):
```typescript
interface SearchResult {
  title: string;
  detail_url: string;
  download_count?: string;
  author?: string;
  langs?: string[];
}
```

# Enhanced interface with format/fps:
```typescript
interface SearchResult {
  title: string;
  detail_url: string;
  download_count?: string;
  author?: string;
  langs?: string[];
  format?: string;   // NEW: SRT, ASS, SSA, SUB
  fps?: string;     // NEW: 24fps, 25fps, etc.
  rating?: string;
}
```

# Existing Zustand store: frontend/src/stores/useUIStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SearchResult interface and API types</name>
  <files>frontend/src/api.ts</files>
  <action>
Update the SearchResult interface in frontend to include format and fps fields:
- Add optional format?: string
- Add optional fps?: string

This ensures TypeScript knows about the new fields from the API.
  </action>
  <verify>
<automated>cd frontend && npm run build 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript recognizes format and fps fields in SearchResult</done>
</task>

<task type="auto">
  <name>Task 2: Create SearchResultDetails component</name>
  <files>frontend/src/components/SearchResultDetails.tsx</files>
  <action>
Create a new component that displays expanded subtitle details:
- Show format badge (e.g., "SRT", "ASS")
- Show FPS (e.g., "24fps")
- Show language tags
- Show author and download count
- Use Tailwind CSS for styling

Export as default component.
  </action>
  <verify>
<automated>ls -la frontend/src/components/SearchResultDetails.tsx</automated>
  </verify>
  <done>SearchResultDetails component renders format, fps, langs, author, download_count</done>
</task>

<task type="auto">
  <name>Task 3: Create SearchResultRow with expandable accordion</name>
  <files>frontend/src/components/SearchResultRow.tsx</files>
  <action>
Create expandable row component:
- Accept SearchResult item and isExpanded, onToggle props
- Use grid-template-rows animation pattern for smooth expand/collapse
- Display title and basic info (langs) in header
- Render SearchResultDetails in expanded content
- Apply Tailwind transition classes for smooth animation

Use accordion behavior: clicking one row should close others.
  </action>
  <verify>
<automated>ls -la frontend/src/components/SearchResultRow.tsx</automated>
  </verify>
  <done>SearchResultRow component renders with expand/collapse</done>
</task>

<task type="auto">
  <name>Task 4: Update SearchPage to use expandable rows with accordion</name>
  <files>frontend/src/pages/SearchPage.tsx</files>
  <action>
Modify SearchPage:
- Import SearchResultRow component
- Add state: expandedIndex (number | null) for accordion behavior
- Update result rendering to use SearchResultRow
- Implement accordion: when clicking a row, set expandedIndex to that index or null if already expanded
- Display format/fps in the results (both collapsed header and expanded details)

Keep existing search UI (input, filters) and download button behavior.
  </action>
  <verify>
<automated>cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>SearchPage displays results as expandable rows with accordion behavior</done>
</task>

</tasks>

<verification>
- SearchResultRow expands/collapses smoothly
- Accordion mode works (only one row expanded at a time)
- Format and FPS displayed in results
</verification>

<success_criteria>
User can view detailed search results with format, FPS, language information (DOWN-01).
User can expand search results to see subtitle details with smooth animation (DOWN-02).
Accordion behavior works correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-manual-download/02-02-SUMMARY.md`
</output>
