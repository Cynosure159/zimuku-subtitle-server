---
phase: 05-backend-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/scraper.py
  - app/core/archive.py
  - app/core/ocr.py
  - app/services/media_service.py
  - app/services/search_service.py
  - app/services/task_service.py
autonomous: true
requirements:
  - REQ-01
  - REQ-03
  - REQ-04

must_haves:
  truths:
    - "Core modules are organized into subdirectories (scraper/, archive/, ocr/)"
    - "Import paths are updated across all dependent files"
    - "All existing functionality remains intact"
  artifacts:
    - path: "app/core/scraper/__init__.py"
      provides: "Scraper module exports"
    - path: "app/core/scraper/agent.py"
      provides: "ZimukuAgent implementation"
    - path: "app/core/archive/__init__.py"
      provides: "Archive module exports"
    - path: "app/core/archive/manager.py"
      provides: "ArchiveManager implementation"
    - path: "app/core/ocr/__init__.py"
      provides: "OCR module exports"
    - path: "app/core/ocr/engine.py"
      provides: "SimpleOCREngine implementation"
  key_links:
    - from: "app/services/media_service.py"
      to: "app/core/scraper"
      via: "import"
      pattern: "from.*core.*scraper"
    - from: "app/services/search_service.py"
      to: "app/core/scraper"
      via: "import"
    - from: "app/services/task_service.py"
      to: "app/core/archive"
      via: "import"
---

<objective>
重构 Core 层目录结构，将 scraper.py, archive.py, ocr.py 转换为子目录模块
</objective>

<execution_context>
@/home/cy/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/core/scraper.py
@app/core/archive.py
@app/core/ocr.py

# Current import pattern (to be updated)
# services/media_service.py: from app.core.scraper import ZimukuAgent
# services/search_service.py: from app.core.scraper import ZimukuAgent
# services/task_service.py: from app.core.archive import ArchiveManager
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scraper subdirectory</name>
  <files>app/core/scraper/__init__.py, app/core/scraper/agent.py</files>
  <action>
1. Create directory app/core/scraper/
2. Move content from app/core/scraper.py to app/core/scraper/agent.py
3. Create app/core/scraper/__init__.py with exports:
   ```python
   from .agent import ZimukuAgent
   __all__ = ["ZimukuAgent"]
   ```
4. Delete original app/core/scraper.py
  </action>
  <verify>
    <automated>python -c "from app.core.scraper import ZimukuAgent; print('OK')"</automated>
  </verify>
  <done>Scraper module reorganized into subdirectory</done>
</task>

<task type="auto">
  <name>Task 2: Create archive subdirectory</name>
  <files>app/core/archive/__init__.py, app/core/archive/manager.py</files>
  <action>
1. Create directory app/core/archive/
2. Move content from app/core/archive.py to app/core/archive/manager.py
3. Create app/core/archive/__init__.py with exports:
   ```python
   from .manager import ArchiveManager
   __all__ = ["ArchiveManager"]
   ```
4. Delete original app/core/archive.py
  </action>
  <verify>
    <automated>python -c "from app.core.archive import ArchiveManager; print('OK')"</automated>
  </verify>
  <done>Archive module reorganized into subdirectory</done>
</task>

<task type="auto">
  <name>Task 3: Create ocr subdirectory</name>
  <files>app/core/ocr/__init__.py, app/core/ocr/engine.py</files>
  <action>
1. Create directory app/core/ocr/
2. Move content from app/core/ocr.py to app/core/ocr/engine.py
3. Create app/core/ocr/__init__.py with exports:
   ```python
   from .engine import SimpleOCREngine
   __all__ = ["SimpleOCREngine"]
   ```
4. Delete original app/core/ocr.py
  </action>
  <verify>
    <automated>python -c "from app.core.ocr import SimpleOCREngine; print('OK')"</automated>
  </verify>
  <done>OCR module reorganized into subdirectory</done>
</task>

<task type="auto">
  <name>Task 4: Update service imports</name>
  <files>app/services/media_service.py, app/services/search_service.py, app/services/task_service.py</files>
  <action>
Update import statements in all services to use new subdirectory paths:
- from app.core.scraper import ZimukuAgent → from app.core.scraper import ZimukuAgent
- from app.core.archive import ArchiveManager → from app.core.archive import ArchiveManager

(Import paths remain the same due to __init__.py re-exports, just verify they work)
  </action>
  <verify>
    <automated>python -c "from app.services.media_service import MediaService; from app.services.search_service import SearchService; from app.services.task_service import TaskService; print('OK')"</automated>
  </verify>
  <done>All service imports work correctly</done>
</task>

</tasks>

<verification>
- All core modules importable from new paths
- All services importable without errors
- ruff check passes
</verification>

<success_criteria>
Core layer reorganized into subdirectories (scraper/, archive/, ocr/), all imports functional
</success_criteria>

<output>
After completion, create .planning/phases/05-backend-refactor/05-01-SUMMARY.md
</output>
